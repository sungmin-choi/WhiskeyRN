import Colors from './colors';

const Fonts = {
  CENTER_16_B_WHITE_700: {
    fontFamily: 'Apple SD Gothic Neo',
    fontSize: 16,
    fontWeight: 'bold',
    fontStyle: 'normal',
    textAlign: 'center',
    color: Colors.WHITE,
  },
} as const;

export default Fonts;
